#SPDX-License-Identifier: MIT-0
---
# defaults file for roles/dns

dnsmasq_upstream_servers:
  - 8.8.8.8
  - 8.8.4.4

dnsmasq_domain: noc.lan
dnsmasq_cache_size: 1000

dnsmasq_listen_address: "{{ ansible_facts['default_ipv4']['address'] }}"

local_a_records: {}